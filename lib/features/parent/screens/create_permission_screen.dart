import 'package:flutter/material.dart';

class CreatePermissionScreen extends StatelessWidget {
  const CreatePermissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buat Izin'),
      ),
      body: const Center(
        child: Text('Create Permission - Coming Soon'),
      ),
    );
  }
}
