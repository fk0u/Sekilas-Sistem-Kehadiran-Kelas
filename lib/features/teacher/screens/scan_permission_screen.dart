import 'package:flutter/material.dart';

class ScanPermissionScreen extends StatelessWidget {
  const ScanPermissionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Scan QR Izin'),
      ),
      body: const Center(
        child: Text('Scan Permission - Coming Soon'),
      ),
    );
  }
}
